
@implements IDialogContentComponent<HistoryName>
<FluentTextField @bind-Value=Content.name Label="History name"></FluentTextField>

@code {
    public HistoryName Content { get; set; } = new HistoryName(); // Ensure SearchContent is properly initialized.
}
